# React Trivia

ENV vars

```
REACT_APP_API_URL - The URL of the API
REACT_APP_GUEST_TOKEN - The authorization token to comunicate with the API
REACT_APP_TIMER_TIME - The amount of time the game timer will have, expressed in seconds. Defaults to 15.
```
